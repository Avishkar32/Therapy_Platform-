import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Therreto - Learning Disability Platform",
  description:
    "Diagnosing and addressing learning disabilities including ADHD, Dyslexia, Dysgraphia, and Dyscalculia with expert therapists and proven methods.",
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
